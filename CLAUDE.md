# Codex 與 Claude 共同協作入口

處理這個 repo 的課程、教案或視覺提案前，先閱讀「協作交接/README.md」。

規則：

1. 先讀進行中的交接項目，再處理內容。
2. 未經 Tarshar 確認，不覆寫原始教材、原始教案、原始印刷檔或既有網頁。
3. 所有改動先另存為建議版本；在交接文件清楚寫出目標、已完成、風險與待決定事項。
4. Claude 收到 Codex 的審查請求時，請在對應的「給Codex」回覆檔直接寫回，不要只回覆在聊天視窗。
5. 不放入個資、帳號資訊、API 金鑰或未授權的課程原文。

6. 動手前先讀專案既有的規則與先例：本檔、「協作交接/README.md」、以及 repo 裡同類型產出物的上一份範本。
   不要在使用者已有格式先例的情況下自創格式，也不要在檔案已在 repo 裡時跑去跟使用者要檔案。
7. 「**AI協作日誌**」有固定正式格式（單檔 HTML、第一人稱、封面＋目錄＋導讀四節＋九部結構），
   範本與技能在 kt-sweet-journey：`docs/*-AI協作日誌.html` 與 `.claude/skills/ai-collab-log/`。
   它不是交接文件、不是研究日誌、不是對話接力棒，不可用索引式清單代替。
